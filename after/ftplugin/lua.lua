print 'After Loaded > Filetype: .lua from: ~/.config/nvim/after/ftplugin/lua.lua'

-- This loads after all the other plugins
-- We specify the language we are writing the config for with the filed
-- name of lua (and the filetype like most nvim configuration is .lua)
-- plugins and settings in the nvim/after folder automatially load after
-- all other plugins and settings
--vim.opt_local.shiftwidth = 4
--vim.opt_local.expandtab = true
--vim.api.nvim_set_hl(0, 'Comment', { fg = '#408FBC', italic = true })
